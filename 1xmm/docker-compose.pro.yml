version: "3.4"

services:
  1xmm_pro_fe:
    container_name: 1xmm_pro_fe
    image: 1xmm_pro_fe:1.0.0
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3400:4173
    restart: unless-stopped
networks:
  default:
